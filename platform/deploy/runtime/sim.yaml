# Runtime overlay: interactive simulation with browser-produced camera frames.
# Switch cam0 to `sim` for the autonomous headless Chromium provider.
active_sources:
  r1: sim
  cam0: browser_sim
  io0: sim
