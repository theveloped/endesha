# Runtime overlay: fully simulated session (arm_sim + headless-browser camera).
# Pair with the canonical deploy/cell.yaml:
#   python -m wf.services.supervisor --cell deploy/cell.yaml --runtime deploy/runtime/sim.yaml
active_sources:
  r1: sim
  cam0: sim
