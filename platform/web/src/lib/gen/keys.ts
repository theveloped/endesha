// GENERATED by wf.tools.wiregen from the Python contract packages —
// do not edit. Regenerate: `pixi run wiregen`; CI drift gate:
// `pixi run wire-check` (wire-contract RFC §7).

// One function per Python key builder; the docstring of each
// Python keys.py stays the normative key table.

// ── wf.contracts.arm.keys ──
export function armPrefix(realm: string, rid: string): string {
  return `${realm}/arm/${rid}`;
}
export function armStateJoints(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/state/joints`;
}
export function armStateFlange(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/state/flange`;
}
export function armStateTcp(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/state/tcp`;
}
export function armStateIo(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/state/io`;
}
export function armStateStatus(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/state/status`;
}
export function armCmdSetDo(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/cmd/set_do`;
}
export function armCmdStop(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/cmd/stop`;
}
export function armCmdClearProtectiveStop(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/cmd/clear_protective_stop`;
}
export function armCmdSetTcp(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/cmd/set_tcp`;
}
export function armCmdJog(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/cmd/jog`;
}
export function armActionPrefix(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/action`;
}
export function armAlive(realm: string, rid: string): string {
  return `${realm}/arm/${rid}/alive`;
}
export function armBaseFrame(rid: string): string {
  return `arm/${rid}/base`;
}

// ── wf.contracts.camera2d.keys ──
export function camera2dPrefix(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}`;
}
export function camera2dImage(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/image`;
}
export function camera2dStateStatus(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/state/status`;
}
export function camera2dCmdConfigure(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/cmd/configure`;
}
export function camera2dCmdGrab(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/cmd/grab`;
}
export function camera2dCmdStreamStart(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/cmd/stream_start`;
}
export function camera2dCmdStreamStop(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/cmd/stream_stop`;
}
export function camera2dAlive(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/alive`;
}
export function camera2dProducerPrefix(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/producer`;
}
export function camera2dProducerCmdAcquire(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/producer/cmd/acquire`;
}
export function camera2dProducerCmdRelease(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/producer/cmd/release`;
}
export function camera2dProducerStateOwner(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/producer/state/owner`;
}
export function camera2dProducerStateDemand(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/producer/state/demand`;
}
export function camera2dProducerIngress(realm: string, cid: string): string {
  return `${realm}/camera2d/${cid}/producer/ingress`;
}
export function camera2dProducerRender(realm: string, cid: string, client_id: string): string {
  return `${realm}/camera2d/${cid}/producer/clients/${client_id}/render`;
}
export function camera2dOpticalFrame(cid: string): string {
  return `camera2d/${cid}/optical`;
}

// ── wf.contracts.control.keys ──
export function controlPrefix(realm: string): string {
  return `${realm}/control`;
}
export function controlCmdAcquire(realm: string): string {
  return `${realm}/control/cmd/acquire`;
}
export function controlCmdRelease(realm: string): string {
  return `${realm}/control/cmd/release`;
}
export function controlStateOwner(realm: string): string {
  return `${realm}/control/state/owner`;
}
export function controlAlive(realm: string): string {
  return `${realm}/control/alive`;
}

// ── wf.contracts.dio.keys ──
export function dioPrefix(realm: string, rid: string): string {
  return `${realm}/dio/${rid}`;
}
export function dioStateChannels(realm: string, rid: string): string {
  return `${realm}/dio/${rid}/state/channels`;
}
export function dioCmdSet(realm: string, rid: string): string {
  return `${realm}/dio/${rid}/cmd/set`;
}
export function dioCmdForce(realm: string, rid: string): string {
  return `${realm}/dio/${rid}/cmd/force`;
}
export function dioAlive(realm: string, rid: string): string {
  return `${realm}/dio/${rid}/alive`;
}

// ── wf.contracts.program.keys ──
export function programCatalog(realm: string): string {
  return `${realm}/programs/catalog`;
}
export function programCmdLoad(realm: string): string {
  return `${realm}/programs/cmd/load`;
}
export function programCmdSource(realm: string): string {
  return `${realm}/programs/cmd/source`;
}
export function programCmdSave(realm: string): string {
  return `${realm}/programs/cmd/save`;
}
export function programCmdDelete(realm: string): string {
  return `${realm}/programs/cmd/delete`;
}
export function programPrefix(realm: string): string {
  return `${realm}/program`;
}
export function programLog(realm: string): string {
  return `${realm}/program/log`;
}
export function programState(realm: string): string {
  return `${realm}/program/state`;
}
export function programCmd(realm: string, command: string): string {
  return `${realm}/program/cmd/${command}`;
}
export function programCmdGlob(realm: string): string {
  return `${realm}/program/cmd/*`;
}
export function programCmdEvent(realm: string): string {
  return `${realm}/program/cmd/event`;
}
export function programTransitions(realm: string): string {
  return `${realm}/program/transitions`;
}
export function programAlive(realm: string): string {
  return `${realm}/program/alive`;
}

// ── wf.contracts.supervisor.keys ──
export function supervisorPrefix(realm: string, node: string): string {
  return `${realm}/supervisor/${node}`;
}
export function supervisorAlive(realm: string, node: string = "main"): string {
  return `${realm}/supervisor/${node}/alive`;
}
export function supervisorDescriptor(realm: string, node: string = "main"): string {
  return `${realm}/supervisor/${node}/descriptor`;
}
export function supervisorDevices(realm: string, node: string = "main"): string {
  return `${realm}/supervisor/${node}/devices`;
}
export function supervisorCmdSetSource(realm: string, node: string = "main"): string {
  return `${realm}/supervisor/${node}/cmd/set_source`;
}
export function supervisorLog(realm: string, service: string, node: string = "main"): string {
  return `${realm}/supervisor/${node}/log/${service.replace(/[/*$?#:]/g, ".")}`;
}
export function supervisorLogGlob(realm: string, node: string = "main"): string {
  return `${realm}/supervisor/${node}/log/*`;
}
export function supervisorEvents(realm: string, node: string = "main"): string {
  return `${realm}/supervisor/${node}/events`;
}

// ── wf.contracts.tags.keys ──
export function tagsPrefix(realm: string, rid: string): string {
  return `${realm}/tags/${rid}`;
}
export function tagsStateTags(realm: string, rid: string): string {
  return `${realm}/tags/${rid}/state/tags`;
}
export function tagsCmdWrite(realm: string, rid: string): string {
  return `${realm}/tags/${rid}/cmd/write`;
}
export function tagsCmdForce(realm: string, rid: string): string {
  return `${realm}/tags/${rid}/cmd/force`;
}
export function tagsAlive(realm: string, rid: string): string {
  return `${realm}/tags/${rid}/alive`;
}

// ── wf.contracts.washer.keys ──
export function washerPrefix(realm: string, rid: string): string {
  return `${realm}/washer/${rid}`;
}
export function washerStateStatus(realm: string, rid: string): string {
  return `${realm}/washer/${rid}/state/status`;
}
export function washerActionPrefix(realm: string, rid: string): string {
  return `${realm}/washer/${rid}/action`;
}
export function washerCmdStopDoor(realm: string, rid: string): string {
  return `${realm}/washer/${rid}/cmd/stop_door`;
}
export function washerCmdGetRecipe(realm: string, rid: string): string {
  return `${realm}/washer/${rid}/cmd/get_recipe`;
}
export function washerCmdSetRecipe(realm: string, rid: string): string {
  return `${realm}/washer/${rid}/cmd/set_recipe`;
}
export function washerAlive(realm: string, rid: string): string {
  return `${realm}/washer/${rid}/alive`;
}

// ── wf.services.config.keys ──
export function configFrame(name: string): string {
  return `config/frames/${name}`;
}
export function configFramesGlob(): string {
  return `config/frames/**`;
}
export function configPose(name: string): string {
  return `config/poses/${name}`;
}
export function configPosesGlob(): string {
  return `config/poses/**`;
}
export function configScene(name: string): string {
  return `config/scene/${name}`;
}
export function configSceneGlob(): string {
  return `config/scene/**`;
}
export function configTcp(rid: string, name: string): string {
  return `config/arm/${rid}/tcp/${name}`;
}
export function configTcpsGlob(rid: string): string {
  return `config/arm/${rid}/tcp/**`;
}
export function configIntrinsics(cid: string): string {
  return `config/intrinsics/${cid}`;
}
export function configIntrinsicsGlob(): string {
  return `config/intrinsics/**`;
}
export function configProgramPose(program: string, name: string): string {
  return `config/programs/${program}/poses/${name}`;
}
export function configProgramPosesGlob(program: string): string {
  return `config/programs/${program}/poses/**`;
}
export function configProgramLayout(program: string): string {
  return `config/programs/${program}/layout`;
}
export function configProgramsGlob(): string {
  return `config/programs/**`;
}
export function configCollisionDisabledPairs(rid: string): string {
  return `config/arm/${rid}/collision/disabled_pairs`;
}
export function configCmdSet(): string {
  return `config/cmd/set`;
}
export function configCmdDelete(): string {
  return `config/cmd/delete`;
}
export function configAlive(): string {
  return `config/alive`;
}

// ── wf.services.recording.keys ──
export function recordingCmdStart(): string {
  return `recording/cmd/start`;
}
export function recordingCmdStop(): string {
  return `recording/cmd/stop`;
}
export function recordingCmdMark(): string {
  return `recording/cmd/mark`;
}
export function recordingState(): string {
  return `recording/state`;
}
export function recordingRecorderAlive(): string {
  return `recording/alive`;
}
export function recordingReplayRealm(session_id: string): string {
  return `replay/${session_id}`;
}
export function recordingReplayCmd(session_id: string, action: string): string {
  return `replay/${session_id}/cmd/${action}`;
}
export function recordingReplayClock(session_id: string): string {
  return `replay/${session_id}/clock`;
}
