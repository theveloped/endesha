[project]
name = "wf-contracts-camera2d"
version = "0.1.0"
description = "WF platform L1: camera2d contract keys, messages, conformance suite"
requires-python = ">=3.12,<3.13"
dependencies = [
    "wf-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wf"]
