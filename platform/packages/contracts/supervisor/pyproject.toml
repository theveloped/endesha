[project]
name = "wf-contracts-supervisor"
version = "0.1.0"
description = "WF platform L1: supervisor / flow-orchestration contract key space"
requires-python = ">=3.12,<3.13"
dependencies = [
    "wf-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wf"]
